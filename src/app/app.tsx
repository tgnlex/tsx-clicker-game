import '../style/app.css'

export function App() {
  const [count, setCount] = useState(0)

  return (
    <main id="app">
      <div id="game-window">
        
        <button id="btn-main">Click Here</button>
      </div>
    </main>
  )
}
